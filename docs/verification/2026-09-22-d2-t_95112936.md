# D2 (t_95112936) verification evidence — candidate 36b1f42

Task: Orbis D2 — GPU-режим в UI: current/queued/reboot-required/Unavailable.
Base: 1253c5f41795d04ac1d9369d91aea18ee93c837e → Candidate: 36b1f42
(workspace branch implementation/current-plan, clean before and after)

## Check verify-task (static_analysis)

Command: `scripts/verify task`
Exit code: 0 ("verification passed")
Content: cargo fmt + cargo check --workspace --all-targets --locked +
cargo test --workspace --all-targets --locked +
cargo clippy --workspace --all-targets --locked -- -D warnings.
Log: /tmp/verify_d2_final.log — 49 test suites, 1513 tests passed, 0 failed.

## Check gpu-ui-states (runtime_state)

PASS — three layers of evidence, all executed on candidate 36b1f42:

1. Presentation-mapper state transitions (8 tests, executed):
   cargo test -p orbis-ui product_gpu
   → 17 passed (8 new status-mapper + 9 pre-existing mutation read-back):
   - product_gpu_status_queued_target_does_not_claim_applied_mode: current
     card selected (gpu_selected=1 Standard), queued card pending
     (gpu_queued=2 Ultimate), reboot_required=true — AC-050.
   - product_gpu_status_queue_equal_to_current_is_not_pending: no phantom
     pending badge when deferred target == observed mode — AC-051.
   - product_gpu_status_unknown_keeps_previous_evidence
   - product_gpu_status_inconsistent_degrades_section_honestly
   - product_gpu_status_outcome_field_mismatch_is_not_success
   - product_gpu_status_ready_state_clears_stale_queue_evidence
   - product_gpu_status_read_never_sets_mode_writable
   - product_gpu_status_read_failure_degrades_only_on_definitive_errors
     (Unsupported/BackendUnavailable → GpuModeHwState::Unavailable, other
     errors keep evidence) — AC-052.
2. Real worker loop (2 tokio tests over a scripted HardwareProductGpuSource,
   executed): cargo test -p orbis-ui --lib worker_loop → 2 passed.
   - worker_loop_publishes_authoritative_product_gpu_status_event: wire
     triple (current=1, queued=2, reboot_required=true) reaches the
     presentation boundary unchanged; exactly one status query per command.
   - worker_loop_reports_unsupported_status_fail_closed: absent promotion
     yields ProviderError::Unsupported, never a fabricated mode.
3. Visual render (ui-review screenshot, executed):
   target/debug/orbis-control --screenshot .../d2-gpu-ui/pending.png
     --ui-section graphics --ui-state pending
   Verified by image inspection: Standard selected (observed mode),
   Ultimate pending with "Требуется перезагрузка", warning line
   "Режим поставлен в очередь · применится при следующем
   выключении/перезагрузке" visible. Screenshot:
   /home/artt/.hermes/profiles/developer/cache/scratch/d2-gpu-ui/pending.png

## Check ac-evidence (unit_test)

- SC-GPU-MODE (current vs queued vs reboot honesty): PASS via
  product_gpu_status_queued_target_does_not_claim_applied_mode +
  product_gpu_status_queue_equal_to_current_is_not_pending + worker loop
  wire-triple test (commands and PASS output above).
- SC-GPU-PARTIAL (independent Unavailable states): PASS via
  product_gpu_status_read_failure_degrades_only_on_definitive_errors;
  additionally the pre-existing per-primitive independence for
  dGPU power/MUX/access (GpuHwState per row) is covered by the existing
  gpu_capabilities_refresh tests in the same suite (all green in
  scripts/verify task). Physical mode-switch on live hardware: MISSING —
  forbidden by the card (no real hardware mode switches) and no ASUS
  hardware in this environment.
- SC-VISUAL-STATES: PASS via ui-review render above (slint files
  intentionally untouched; the view already rendered all four states —
  this card wired the data path).

## Environment note

Hardware1ProductGpuStatus on the live system bus requires orbis-hardwared
plus an ASUS asusd peer; end-to-end runtime verification used the
production worker loop with a scripted HardwareProductGpuSource and the
ui-review offscreen renderer, per the card's prohibition on real
hardware switches.
