name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  flake-check:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "full nix flake check intentionally skipped for targeted validation"
