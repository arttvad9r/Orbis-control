# Orbis Control — DECISIONS

## D-001: GitHub main — canonical baseline

Решение:

Не продолжать работу вслепую в старом local checkout с неизвестным состоянием.

Если текущий checkout не доказанно чистый и синхронизированный, сохранить его нетронутым и создать свежий sibling clone GitHub `main`.

Почему:

Это безопаснее, чем переименовывать, очищать или удалять неизвестный repository, и исключает влияние старых незакоммиченных экспериментов.

---

## D-002: Сохраняем Rust + Slint и текущие privilege boundaries

Решение:

Не переписывать Orbis Control и не менять основной stack/daemon boundaries без доказанной необходимости.

Почему:

Существующий проект уже содержит production paths, tests и hardware safety invariants. Переписывание не решает текущую проблему завершённости продукта и резко увеличивает regression risk.

---

## D-003: Новый набор документов — источник истины текущей итерации

Решение:

Для текущей продуктовой итерации:

- `SPEC.md` — требования продукта;
- `ACCEPTANCE.md` — объективные критерии готовности;
- `IMPLEMENTATION_PLAN.md` — execution queue;
- `DECISIONS.md` — устойчивые решения.

Эти документы имеют приоритет над старыми roadmap/completion documents для пересекающегося scope.

Старый `FINISH_PLAN.md` нельзя использовать как параллельную независимую очередь задач.

Если `AGENTS.md` или другие инструкции отправляют агента в `FINISH_PLAN.md`, необходимо минимально обновить такие ссылки, чтобы для текущего scope существовала одна активная execution queue.

Почему:

Несколько параллельных roadmap создают противоречия и провоцируют работу над планированием вместо реализации.

---

## D-004: TDP представлен реальными hardware fields

Решение:

Не вводить единое синтетическое значение TDP.

Показывать реальные SPL/SPPT/FPPT и другие capability fields отдельно.

Почему:

Они имеют разные hardware semantics, диапазоны и lifecycle. Объединение ухудшает точность, read-back и диагностику.

---

## D-005: Capability-driven вместо model-driven

Решение:

Controls появляются и становятся writable на основании runtime evidence.

Почему:

Одинаковая модель ноутбука не гарантирует одинаковый firmware/driver/backend interface. Проект уже использует capability-oriented approach.

---

## D-006: Существующий audited UI — базовый visual language

Решение:

Не проектировать новый интерфейс с нуля.

Использовать существующие audited Slint pages, Palette и components и исправлять конкретные visual/UX defects.

Почему:

Основная проблема — законченность реализации и качество конкретных состояний, а не отсутствие ещё одной дизайн-концепции.

---

## D-007: Real hardware validation отделена от implementation

Решение:

Реальные hardware writes не являются обязательными для продолжения разработки, пока пользователь отдельно их не разрешил.

Почему:

Software path можно реализовывать и проверять через unit, fixture, private-D-Bus и P2P integration tests. Отдельное разрешение требуется для утверждения, что конкретная mutation live-validated на физическом устройстве.

---

## D-008: Разрешение на bounded live-hardware round-trip validation

Решение:

Пользователь заранее разрешил bounded real-hardware round-trip валидацию на целевой машине (ASUS TUF Gaming A17) для финального релизного гейта v0.1. Условия: каждое изменённое значение восстанавливается после теста; валидация выполняется только для включённых mutation paths на release candidate.

Почему:

Целевая машина доступна, `asusd` активен. Без live-валидации релизная претензия «controls work end to end» недоказуема.

---

## D-009: Цель итерации — первый релиз v0.1

Решение:

Цель подтверждена: installable Arch-пакет (`packaging/arch/PKGBUILD`), installed smoke на целевой машине, bump версии и первый релиз после прохождения ACCEPTANCE.md. Draft PR #130 (`agent/finish-v01`) закрыт как устаревший — работа продолжается в ветке `implementation/current-plan`.

Почему:

Совпадает с finish target в `FINISH_PLAN.md`; устаревший PR создавал вторую конкурирующую линию работ.

---

## D-010: Remote-политика текущей итерации

Решение:

Ветка `implementation/current-plan` пушится в origin для сохранности работы. `main` не переписывается и не продвигается вперёд до готовности release candidate.

Почему:

Защищает локальные коммиты (power-limit pipeline и docs) от потери, сохраняя `main` стабильным базовым уровнем.

---

## D-011: G-Helper-linux — основной референс бэкенда

Решение:

Основной технический референс для backend-решений Orbis — G-Helper-linux, локальная копия `/home/artt/g-helper-linux` (upstream `utajum/g-helper-linux`). Когда реализация в Orbis буксует (диапазоны, sysfs-атрибуты, fan control, GPU modes, power management), воркер обязан изучить соответствующий модуль референса (`src/Platform/Linux/Asus/`, `src/Fan/`, `src/Gpu/`, `src/Battery/`, `src/Mode/`) и найти решение там, прежде чем изобретать своё. Референс не лицензионно-обязывающий: решения адаптируются под архитектуру Orbis (Rust, capability-driven, Hardware1), а не копируются вслепую.

Почему:

Референс установлен и покрывает весь целевой функционал Orbis на том же классе железа; это дешевле и надёжнее самостоятельного исследования.

## D-012. Полный функциональный паритет с G-Helper (2026-09-22, locked)

**Решение:** Orbis реализует весь функционал G-Helper (Windows) на Linux. Референс —
G-Helper Windows + g-helper-linux v1.0.92 (доказательство реализуемости на Linux).
«Без оправданий»: каждая фича-группа входит в объём; доступность на конкретной машине
определяется capability-discovery (честный Unsupported), а не исключением из объёма.
SPЕC-запреты на automation/import-export, конфликтующие с паритетом, снимаются этим
решением (пользователь явно расширил объём); архитектурные инварианты (GUI без root,
Hardware1, честные состояния) не отменяются.

**Матрица паритета и волны P1..P6:** docs/parity/g-helper-parity.md.
Волны стартуют после релиза v0.1 (карта R).
